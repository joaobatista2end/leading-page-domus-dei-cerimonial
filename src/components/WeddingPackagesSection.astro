---
import PackageCard from './PackageCard.astro';
import packages from '../data/packages.json';
---

<section id="pacotes" class="bg-orange-100 py-12">
  <div class="max-w-screen-lg mx-auto flex-col flex justify-center">
    <div class="flex flex-col items-center gap-2 mb-8">
      <h2 class="font-poppins font-[100] text-4xl text-black/80" data-aos="fade-down">
        Casamento
      </h2>
      <img
        src="images/decoration_1.png"
        alt="decoration"
        class="w-[200px] -mt-4"
        data-aos="zoom-in"
        data-aos-delay="200"
      />

      <p
        class="font-thin text-sm max-w-[350px] text-center hyphens-auto"
        lang="pt-br"
        data-aos="fade-up"
        data-aos-delay="300"
      >
        Conheça nossos pacotes de casamento. Cada um é personalizado para
        atender às suas necessidades e expectativas.
      </p>
    </div>

    <div class="max-w-[840px] mx-auto grid grid-cols-1 md:grid-cols-3 gap-4">
      {
        packages.map((pkg, index) => (
          <div data-aos="fade-up" data-aos-delay={400 + index * 100}>
            <PackageCard
              name={pkg.name}
              description={pkg.description}
              services={pkg.services}
              icon={pkg.icon}
            />
          </div>
        ))
      }
    </div>
  </div>
</section> 