---
import { Instagram, Facebook, Mail, Phone, PhoneCall } from "lucide-astro";
import social from "../data/social.json";

const iconMap = {
  Instagram: Instagram,
  Facebook: Facebook,
  WhatsApp: PhoneCall,
  "E-mail": Mail,
};
---

<footer class="bg-black/90 text-white/60 py-8 text-xs" id="contato">
  <div class="flex flex-col max-w-xs mx-auto">
    <img
      src="images/logo-90x90.svg"
      alt="Domus Dei"
      class="h-16 mb-4 object-contain"
    />
    <p class="font-thin hyphens-auto mb-2" lang="pt-br">
      Transformamos sonhos em realidade com dedicação, propósito e muito amor.
      Cuidamos dos mínimos detalhes para que você viva o momento com leveza.
    </p>
    <p class="font-thin mb-4">
      <strong>Endereço:</strong> Rua João XXIII, 123 - Jardim das Indústrias - São Paulo/SP
    </p>

    <div class="flex gap-4 mx-auto">
      {
        social.map((item) => (
          <a
            href={item.url}
            target="_blank"
            rel="noopener noreferrer"
            class="w-10 h-10 bg-zinc-800 rounded-full flex items-center justify-center hover:bg-orange-300/20 transition-colors"
            aria-label={item.name}
          >
            {item.name === "Instagram" && <Instagram size={20} />}
            {item.name === "Facebook" && <Facebook size={20} />}
            {item.name === "WhatsApp" && <Phone size={20} />}
            {item.name === "E-mail" && <Mail size={20} />}
          </a>
        ))
      }
    </div>
  </div>
  <div class="max-w-screen-lg mx-auto px-6">
    <!-- Separador -->
    <div class="border-t border-zinc-800 my-8"></div>

    <!-- Copyright -->
    <div class="text-center text-xs text-zinc-400 font-thin">
      <p>
        © {new Date().getFullYear()} <strong>Domus Dei Cerimoniais</strong>. Todos os direitos reservados.
      </p>
      <p class="mt-1">
        <img
          src="images/decoration_1.png"
          alt="Decoração"
          class="h-4 inline-block opacity-30"
        />
      </p>
    </div>
  </div>
</footer>
